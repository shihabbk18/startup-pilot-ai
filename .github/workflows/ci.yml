name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  backend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: startup-pilot-ai/backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: python -m pip install --upgrade pip
      - run: pip install ".[dev]"
      - run: ruff check app
      - run: pytest ../tests/backend

  frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: startup-pilot-ai/frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: startup-pilot-ai/frontend/package.json
      - run: npm install
      - run: npm run typecheck
      - run: npm run build

